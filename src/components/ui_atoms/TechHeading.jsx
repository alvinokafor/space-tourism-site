import React from 'react'

export default function TechHeading({name}) {
  return (
    <h4 className='mt-2 font-title uppercase text-[1.5rem] md:text-[2.5rem] text-white lg:text-2xl'>{name}</h4>
  )
}
